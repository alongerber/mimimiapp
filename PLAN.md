# תוכנית פעולה מפורטת - מי מי מי?

## 1. הסרת הודעות "רוצים לדבר על זה"
**קבצים:** `index.html` שורות 1500, 1871
**בעיה:** אחרי חשיפת תשובות, המערכת כותבת "רוצים לדבר על זה? קחו רגע." — זה הורג את הזרימה ונותן הנחיה מיותרת.
**פתרון:** הסרת שתי ההופעות של הטקסט הזה לחלוטין. גם הסרת `showBreathe` (שורה ~1551) שמציג "מישהו רוצה להוסיף משהו? קחו את הזמן." — במקום זה, ישר advance().

## 2. הסרת אימוג'ים של וואטסאפ/Unicode
**קבצים:** `index.html` — חיפוש גלובלי
**בעיה:** שימוש באימוג'ים Unicode (👍👎🧒🎒🎧🎭⏰ וכו') במקום SVG icons.
**פתרון:** החלפת כל האימוג'ים Unicode ב-SVG icons מותאמים או בטקסט עברי. דוגמאות:
- `👍 שכנע` → טקסט בלבד "שכנע!"
- `👎 לא קנית אותי` → טקסט בלבד
- אימוג'ים בכפתורי הגיל (🧒🎒🎧🎭) → SVG או טקסט
- `⏰` בטיימר → SVG שעון
- `✅` → SVG check
- `🗳🔥💬🎨🎤⚖💕🎯` בפידבק → SVG icons (כבר קיימים כ-IC)

## 3. שילוב הלוגו במסך הכניסה
**קבצים:** `index.html` שורות 479-493 (hero section), קובץ לוגו חיצוני
**בעיה:** מסך הכניסה מציג SVG פשוט של עיגולים וסימן שאלה, לא את הלוגו הרשמי.
**פתרון:**
- שמירת הלוגו שסופק כ-`logo.png` ב-public
- החלפת ה-SVG הנוכחי (`hero-vis`) בתמונה של הלוגו עם אפקט glow/shadow יוקרתי
- הוספת CSS animation עדינה (fade-in + scale) ללוגו
- הסרת הטקסט "מי מי מי?" מה-hero-t כי הלוגו כבר מכיל אותו

## 4. שיפור מנגנון הציטוט — הבהרת המטרה
**קבצים:** `index.html` שורות 1515, 1543, 1730-1731
**בעיה:** כפתור "ציטוט!" לא מסביר מה הוא עושה, מתי להשתמש, ולמה.
**פתרון:**
- שינוי הכפתור מ-"ציטוט!" ל-"משפט קילר נאמר? שמרו אותו!"
- בפעם הראשונה שנלחץ, הוספת tooltip/explain קצר: "שמעתם משפט מצחיק או מרגש? כתבו אותו וזה יופיע בסיכום הערב!"
- שינוי placeholder מ-"הכניסו ציטוט..." ל-"מה נאמר פה? כתבו את המשפט..."
- הוספת שם השחקן שאמר את זה (dropdown קצר)

## 5. הוספת הנחיות ברורות בכל שלב — מי מצביע ומתי
**קבצים:** `index.html` — כל פונקציות הסיבוב
**בעיה:** לא ברור למשתמשים מה נדרש מהם — מי מצביע, מתי, ואיך.
**פתרון:** הוספת הנחיה ויזואלית ברורה בכל סיבוב:
- **הצבעה:** "כולם מצביעים! כל אחד בוחר שם" + אייקון שמדגיש
- **כיסא חם:** "{שם} — קראו את השאלה בקול וענו. השאר מקשיבים"
- **דילמה:** "{שם} — קראו בקול את שתי האפשרויות, אחר כך כל אחד בוחר"
- **ברק:** "{שם} — אתם מדברים בקול! אמרו תשובות כמה שיותר מהר"
- **משימה:** "{שם} — בחרו אתגר. השאר שופטים אם הצלחתם"
- **גלה/י (reveal):** "{שם1} ו-{שם2} — עשו את מה שכתוב ביחד!"
- **blind:** הסבר מי עונה ראשון ומי שני

## 6. ניסוח תוצאות ללא אחוזים — שפה מרגשת וג'וסית
**קבצים:** `index.html` — `dlReveal`, `voteReveal`, `pdReveal`
**בעיה:** "67% לא לדעת / 33% לדעת" — יבש, לא מרגש, ילדים לא מתחברים.
**פתרון:** החלפת תצוגת אחוזים בניסוחים כמו:
- "רוב המשפחה בחרה ב..." / "המשפחה חלוקה!"
- הוספת תגובות דינמיות: "פה אחד!", "כמעט כולם!", "מפתיע — הרוב אומר...", "המשפחה חלוקה — ויכוח בדרך!"
- שמירת ה-bar הויזואלי אבל בלי מספרי אחוזים
- הוספת אנימציית "חשיפה" דרמטית (countdown 3-2-1 לפני תוצאה)

## 7. תוכן יותר ג'וסי — דינמיקה משפחתית
**קבצים:** `index.html` — מאגרי שאלות (HOTSEAT, votes, REVEAL)
**בעיה:** תוכן פילוסופי מדי, ילדים לא מתחברים. חסרות שאלות על דינמיקה משפחתית אמיתית.
**פתרון:** הוספת 20+ שאלות חדשות בסגנון:
- "מי הכי אוהב לישון?"
- "המקרר התרוקן בלילה מגלידות — מי החשוד המרכזי?"
- "מי תמיד מפספס את הפח?"
- "מי הכי מתעצל לקום בבוקר?"
- "מי אוכל הכי הרבה בארוחת שישי?"
- "מי הכי טוב בלמצוא תירוצים?"
- "מי שורד הכי פחות בלי WiFi?"
- "מי הכי דרמטי/ת במשפחה?"
- "מי היה שולח את כולם לישון הכי מוקדם?"
- "מי הכי מתלונן על הקור/חום?"
- "של מי החדר הכי מבולגן?"
- "מי אוהב הכי הרבה סלפי?"
- "מי הכי מפונק/ת?"
- "מי הכי כועס כשמפסיד במשחק?"

## 8. תיקון סיבוב הגלגל — עצירה חלקה בלי קפיצה
**קבצים:** `index.html` שורות 1295-1310
**בעיה:** הגלגל מסתובב, נעצר על שחקן אחד, ואז קופץ לשחקן אחר בגלל קוד ה-nudge.
**פתרון:**
- תיקון חישוב הזווית הסופית כך שה-animation תמיד נוחתת בדיוק על השחקן הנכון
- הסרת/שיפור ה-nudge correction (שורה 1302) — במקום קפיצה, חישוב ה-`desR` נכון מראש
- שינוי `midF=.3+Math.random()*.4` כך שתמיד ייחת במרכז ה-slice
- הוספת אנימציית easing חלקה יותר בשניות האחרונות

## 9. הכנה לסיבובי ברק (אתגרי זמן)
**קבצים:** `index.html` — `showRapid` (שורה 1633)
**בעיה:** אתגר "3 מדינות שמתחילות ב-א" מתחיל מיד — השחקן לא מוכן. גם הזמן קצר מדי.
**פתרון:**
- הוספת מסך "הכנה" לפני התחלת הטיימר: "אתגר ברק בדרך! {שם} — כשאתם מוכנים, לחצו התחל!"
- הטיימר מתחיל רק אחרי לחיצה על "מוכנים? יאללה!"
- הגדלת זמן ברירת מחדל ב-5 שניות (מ-`q.t` ל-`q.t + 5`)

## 10. תיקון משימת החיקוי — ניסוח ולוגיקה
**קבצים:** `index.html` שורה 803
**בעיה:** "%1, חקה את %2 — כולם מנחשים מי!" — לא ברור מה "כולם מנחשים מי", ועלול להיות מעליב בין ילדים/בני דודים.
**פתרון:**
- שינוי ניסוח: "%1, תעשה חיקוי של %2 — ובוא נראה אם כולם מזהים!"
- הוספת לוגיקה: %2 תמיד יהיה שחקן מבוגר (parent/grandparent) — לא ילד מחקה ילד אחר
- סינון: אם כל השחקנים ילדים (age 6-13), שינוי המשימה לחיקוי דמות מוכרת

## 11. שיפור ניסוח ניתוח השכנוע (מיקרופון)
**קבצים:** `index.html` שורות 2013, 2049, 2110-2112
**בעיה:** "חשפו את הניתוח!", "ניתוח בינה מלאכותית" — ניסוח טכני ומנוכר. גם לא ברור אם ה-AI באמת עבד.
**פתרון:**
- שינוי "חשפו את הניתוח!" → "מה אומר השופט?"
- שינוי "ניתוח בינה מלאכותית" → "השופט מחליט!"
- שינוי "הבינה המלאכותית מנתחת את הנאום..." → "השופט שוקל את הטענה..."
- הוספת אינדיקטור ויזואלי האם ה-AI באמת ענה (אם fallback — הראה "פסק דין אוטומטי" במקום)
- הוספת `console.log` או badge קטן לדבאג שמראה אם ה-API עבד

## 12. תיקון "מה הדבר שהכי רוצה שיזכרו עליך" — הסרת בחירת משתמש
**קבצים:** `index.html` שורה 1064, `showPDilemma` (1747)
**בעיה:** השאלה הזו אישית — המשתמש עונה על עצמו. לא צריך לבחור שחקן אחר.
**פתרון:**
- שאלות עם `opts:[]` ב-PDILEMMA שהן אישיות — צריכות להישאל כ-hotseat (ענה בקול) במקום כבחירה משחקן
- העברת שאלות אישיות ל-HOTSEAT pool
- או: שינוי הלוגיקה ב-showPDilemma — אם `opts` ריק, הצג כשאלה פתוחה (כמו כיסא חם) במקום לבקש לבחור שם

## 13. רווח אישי לשחקן התורן
**קבצים:** `index.html` — `showRound`, כל פונקציות הסיבוב
**בעיה:** השחקן התורן "נותן" לאחרים אבל לא מרגיש רווח אישי.
**פתרון:**
- בסיבובי הצבעה: השחקן התורן בוחר ראשון + מקבל נקודה על תרומה
- בכיסא חם: כבר קיים (נקודות על תשובה) — תקין
- בדילמות: השחקן שקורא מקבל נקודה + "איזו דילמה שבחרת!"
- הוספת micro-reward: אנימציה קצרה + טקסט כמו "תודה {שם}! +1 על תרומה"
- הוספת מנגנון "שאלת בונוס" — כל 3-4 סיבובים השחקן התורן מקבל שאלה קטנה אישית שלו

## סדר עדיפויות לביצוע:
1. **קריטי (UX שבור):** #8 גלגל, #5 הנחיות, #9 הכנה לברק
2. **חשוב (חוויה):** #1 הסרת "קחו רגע", #6 ניסוח תוצאות, #7 תוכן ג'וסי, #10 חיקוי
3. **שיפור (polish):** #3 לוגו, #2 אימוג'ים, #4 ציטוט, #11 שכנוע, #12 שיזכרו עליך, #13 רווח אישי
